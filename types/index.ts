// undefined: when a field might be missing or not set

// null: when you want to explicitly state "no value" or "not applicable"

// Required fields: when the field must always have a value (neither undefined nor null).

// undefined | null: Use when a field may be either absent (undefined) or explicitly no value (null).

export * from "./user";
export * from "./location";
export * from "./listing";
